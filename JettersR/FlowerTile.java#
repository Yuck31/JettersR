package Rain;

public class FlowerTile extends Tile
{
    public FlowerTile(Sprite sprite)
    {
        super(sprite);
    }

    public void render(int x, int y, Screen screen)//Renders Tile
    {
        screen.renderTile(x << 5, y << 5, this);
    }
}
